package modi.constructor.pac2;

import modi.constructor.pac1.*;

public class C {
	
	A a1=new A(true); //public 생성자 호출(0)
//	A a2=new A(1); //p.f 생성자 호출(X) p,f 제한자는 다른패키지에서 설령 import를 했다고해도 쓸 수가 없다. 
//	A a=new A("메롱"); //private 생성자 호출(X)
	
	

}
